# Database - PostgreSQL (production) or SQLite (development)
# PostgreSQL:
# DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@localhost:5432/snooker_scoring
# SQLite (no setup required):
# DATABASE_URL=sqlite:///./snooker_scoring.db
