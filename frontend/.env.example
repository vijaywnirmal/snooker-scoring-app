# Backend API URL (required for production/Vercel)
# Local dev: leave unset to use /api (Vite proxy)
# Vercel: set in Project Settings → Environment Variables
VITE_API_URL=https://your-backend-url.railway.app
